# Description + Hashtags Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Générer en local une description TikTok (accroche + bénéfices + CTA + hashtags) depuis la transcription, affichée/éditable dans un onglet "Description" de l'app.

**Architecture:** Module Python `caption.py` (templates, sans IA) ; exposé via `/load` (champ `caption`) et `POST /caption` ; onglet Description dans le frontend (textarea + Copier + Régénérer).

**Tech Stack:** Python (regex/templates), FastAPI, Electron/JS.

---

## File Structure
- `backend/config.py` — ajout `BASE_HASHTAGS`, `DEFAULT_BENEFITS`, `DEFAULT_CTA`, `BRAND_TAGS`, `BENEFIT_KEYWORDS`.
- `backend/pipeline/caption.py` — **nouveau** : `generate_caption(text)`.
- `backend/service.py` — `load_audio` ajoute `caption` ; nouvelle fonction `caption(text)`.
- `backend/server.py` — endpoint `POST /caption`.
- `frontend/` — onglet Description (index.html, styles.css, renderer.js, preload.js).

---

## Task 1: Réglages config

**Files:** Modify `backend/config.py`

- [ ] **Step 1: Ajouter à la fin de `backend/config.py`**

```python
DEFAULT_CTA = "Écris-moi en commentaire 👇"
BASE_HASHTAGS = ["#montre", "#montrehomme", "#seikomod", "#horlogerie", "#watch",
                 "#watchlover", "#pourtoi", "#fyp", "#luxe", "#style"]
DEFAULT_BENEFITS = ["✅ Qualité qui en jette", "✅ Prix accessible", "✅ Livraison rapide"]
BENEFIT_KEYWORDS = ["prix", "qualité", "livraison", "saphir", "acier", "24h", "€",
                    "euro", "garantie", "automatique", "mouvement", "bracelet", "cadran"]
BRAND_TAGS = {"rolex": "#rolex", "omega": "#omega", "cartier": "#cartier",
              "seiko": "#seiko", "patek": "#patek", "audemars": "#audemarspiguet",
              "tissot": "#tissot", "tag": "#tagheuer", "heuer": "#tagheuer",
              "daytona": "#daytona", "submariner": "#submariner", "datejust": "#datejust"}
```

- [ ] **Step 2: Commit**
```bash
git add backend/config.py
git commit -m "feat(caption): config (hashtags, benefits, CTA, brand tags)"
```

---

## Task 2: Module caption

**Files:** Create `backend/pipeline/caption.py`, Test `backend/tests/test_caption.py`

- [ ] **Step 1: Écrire le test (échec attendu)**

```python
# backend/tests/test_caption.py
from backend.pipeline.caption import generate_caption

TEXT = ("Pourquoi t'as pas de Rolex ? Parce que le prix fait peur. "
        "La qualité est au rendez-vous avec un verre saphir. "
        "Livraison en 24h. Écris SEIKO en commentaire.")

def test_accroche_is_first_sentence():
    c = generate_caption(TEXT)
    assert c["description"].splitlines()[0].startswith("Pourquoi t'as pas de Rolex")

def test_brand_hashtag_added():
    c = generate_caption(TEXT)
    assert "#rolex" in c["hashtags"]
    assert "#seiko" in c["hashtags"]

def test_cta_detected():
    c = generate_caption(TEXT)
    assert "commentaire" in c["description"].lower()

def test_benefits_prefixed():
    c = generate_caption(TEXT)
    assert "✅" in c["description"]

def test_hashtags_dedup_and_full():
    c = generate_caption(TEXT)
    assert len(c["hashtags"]) == len(set(c["hashtags"]))
    assert c["full"].endswith(" ".join(c["hashtags"]))

def test_empty_text_does_not_crash():
    c = generate_caption("")
    assert isinstance(c["full"], str)
```

- [ ] **Step 2: Lancer (échec)** — `pytest backend/tests/test_caption.py -v` → FAIL (import).

- [ ] **Step 3: Écrire `backend/pipeline/caption.py`**

```python
import re
from backend.config import (DEFAULT_CTA, BASE_HASHTAGS, DEFAULT_BENEFITS,
                            BENEFIT_KEYWORDS, BRAND_TAGS)

CTA_VERBS = ("écris", "ecris", "commente", "commande", "abonne", "suis", "clique")

def _sentences(text):
    parts = re.split(r"(?<=[.!?…])\s+", text.replace("\n", " ").strip())
    return [p.strip() for p in parts if p.strip()]

def _is_cta(s):
    sl = s.lower()
    return any(v in sl for v in CTA_VERBS)

def _is_benefit(s):
    sl = s.lower()
    return any(k in sl for k in BENEFIT_KEYWORDS)

def generate_caption(text):
    sents = _sentences(text)
    accroche = sents[0] if sents else ""
    cta = next((s for s in sents if _is_cta(s)), DEFAULT_CTA)
    benefits = [s for s in sents[1:] if _is_benefit(s) and s != cta][:3]
    benefits = ["✅ " + b for b in benefits] or list(DEFAULT_BENEFITS)

    tl = text.lower()
    tags = list(BASE_HASHTAGS)
    for brand, tag in BRAND_TAGS.items():
        if re.search(r"\b" + re.escape(brand) + r"\b", tl) and tag not in tags:
            tags.append(tag)
    # dédoublonnage en gardant l'ordre, max 12
    seen = set(); hashtags = []
    for t in tags:
        if t not in seen:
            seen.add(t); hashtags.append(t)
    hashtags = hashtags[:12]

    lines = []
    if accroche: lines.append(accroche)
    if benefits: lines += ["", *benefits]
    lines += ["", cta]
    description = "\n".join(lines).strip()
    full = description + "\n\n" + " ".join(hashtags)
    return {"description": description, "hashtags": hashtags, "full": full.strip()}
```

- [ ] **Step 4: Lancer (succès)** — `pytest backend/tests/test_caption.py -v` → PASS (6).

- [ ] **Step 5: Commit**
```bash
git add backend/pipeline/caption.py backend/tests/test_caption.py
git commit -m "feat(caption): local description + hashtags generator"
```

---

## Task 3: Service + endpoint

**Files:** Modify `backend/service.py`, `backend/server.py`, Test `backend/tests/test_server.py`

- [ ] **Step 1: `service.py` — importer caption et l'ajouter à load_audio**

En haut, ajouter `caption` à l'import pipeline :
```python
from backend.pipeline import audio_clean, align, subtitles, montage, detect, waveform, sfx_plan, caption
```
Dans `_analyze`, avant le `return`, ajouter la clé caption :
```python
    res_caption = caption.generate_caption(transcript)
    return {
        "clean_path": clean_path,
        "duration": duration,
        "transcript": transcript,
        "words": [asdict(w) for w in words],
        "detect": detect.detect(words),
        "peaks": waveform.peaks(clean_path),
        "caption": res_caption,
    }
```
(Remplace le `return {...}` existant par celui-ci.)

Ajouter une fonction à la fin de `service.py` :
```python
def make_caption(text):
    return caption.generate_caption(text)
```

- [ ] **Step 2: `server.py` — endpoint `/caption`**

Ajouter le modèle (près des autres) :
```python
class CaptionReq(BaseModel):
    text: str
```
Ajouter l'endpoint (près de `/load`) :
```python
@app.post("/caption")
def caption_ep(req: CaptionReq):
    return service.make_caption(req.text)
```

- [ ] **Step 3: Test serveur — ajouter dans `backend/tests/test_server.py`**

```python
def test_caption_endpoint():
    r = client.post("/caption", json={"text": "Pourquoi une Seiko ? Le prix est top. Écris SEIKO en commentaire."})
    data = r.json()
    assert "#seiko" in data["hashtags"]
    assert "full" in data and data["full"]
```

- [ ] **Step 4: Lancer** — `pytest backend/tests/test_server.py::test_caption_endpoint -v` → PASS.

- [ ] **Step 5: Commit**
```bash
git add backend/service.py backend/server.py backend/tests/test_server.py
git commit -m "feat(caption): expose caption via /load and /caption"
```

---

## Task 4: UI — onglet Description

**Files:** Modify `frontend/preload.js`, `frontend/index.html`, `frontend/styles.css`, `frontend/renderer.js`

- [ ] **Step 1: preload — ajouter `caption`**

Dans `frontend/preload.js`, ajouter dans l'objet exposé :
```javascript
  caption: (text)                              => post('/caption', { text }),
```

- [ ] **Step 2: index.html — onglets + zone description**

Remplacer le bloc `<div class="panel right"> ... </div>` par :
```html
      <div class="panel right">
        <div class="tabs">
          <button id="tabTr" class="tab active">Transcription</button>
          <button id="tabDesc" class="tab">Description</button>
        </div>
        <div id="paneTr" class="pane">
          <textarea id="transcript" placeholder="La transcription apparaîtra ici…"></textarea>
        </div>
        <div id="paneDesc" class="pane" style="display:none">
          <textarea id="caption" placeholder="La description apparaîtra ici…"></textarea>
          <div class="descbtns">
            <button id="regenBtn" class="tool">↻ Régénérer</button>
            <button id="copyBtn" class="tool">📋 Copier</button>
          </div>
        </div>
      </div>
```

- [ ] **Step 3: styles.css — onglets**

Ajouter :
```css
.tabs { display:flex; gap:6px; margin-bottom:8px; }
.tab { background:#222; border:1px solid #333; color:#bbb; padding:6px 12px; border-radius:6px 6px 0 0; cursor:pointer; font-size:13px; }
.tab.active { background:#161617; color:#4ad7d1; border-bottom-color:#161617; }
.pane { flex:1; display:flex; flex-direction:column; min-height:0; }
#caption { flex:1; background:#0f0f10; color:#dcdce0; border:1px solid #2a2a2d; border-radius:8px; padding:12px; font-size:14px; line-height:1.7; resize:none; }
.descbtns { display:flex; gap:8px; margin-top:8px; }
```

- [ ] **Step 4: renderer.js — logique onglets + caption**

En haut, après `const transcript = ...`, ajouter les éléments :
```javascript
const captionBox = document.getElementById('caption');
const tabTr = document.getElementById('tabTr');
const tabDesc = document.getElementById('tabDesc');
const paneTr = document.getElementById('paneTr');
const paneDesc = document.getElementById('paneDesc');
const regenBtn = document.getElementById('regenBtn');
const copyBtn = document.getElementById('copyBtn');

function showTab(which) {
  const desc = which === 'desc';
  paneDesc.style.display = desc ? 'flex' : 'none';
  paneTr.style.display = desc ? 'none' : 'flex';
  tabDesc.classList.toggle('active', desc);
  tabTr.classList.toggle('active', !desc);
}
tabTr.addEventListener('click', () => showTab('tr'));
tabDesc.addEventListener('click', () => showTab('desc'));

regenBtn.addEventListener('click', async () => {
  try {
    const c = await window.api.caption(transcript.value);
    if (c.error) throw new Error(c.error);
    captionBox.value = c.full;
  } catch (e) { captionBox.value = 'Erreur : ' + (e.message || e); }
});
copyBtn.addEventListener('click', async () => {
  await navigator.clipboard.writeText(captionBox.value);
  copyBtn.textContent = '✓ Copié';
  setTimeout(() => { copyBtn.textContent = '📋 Copier'; }, 1200);
});
```

Dans `setState(res)`, à la fin, pré-remplir la description :
```javascript
  if (res.caption) captionBox.value = res.caption.full;
```

- [ ] **Step 5: Vérifier la syntaxe**
Run: `cd frontend && for f in preload.js renderer.js; do node --check "$f"; done`
Expected: aucune erreur.

- [ ] **Step 6: Commit**
```bash
git add frontend/preload.js frontend/index.html frontend/styles.css frontend/renderer.js
git commit -m "feat(caption): Description tab (editable + copy + regenerate)"
```

---

## Task 5: Vérification finale

- [ ] **Step 1: Suite complète** — `pytest backend/tests/ -q` → tous PASS.
- [ ] **Step 2: Test manuel** — `cd frontend && npm start` : déposer un audio → onglet **Description** pré-rempli (accroche + ✅ bénéfices + CTA + hashtags avec la marque) ; **Régénérer** après édition du texte ; **Copier** met dans le presse-papiers.

---

## Self-Review
- **Couverture spec :** génération locale (T2) · accroche/bénéfices/CTA/hashtags+marque (T2) · `/load`+`/caption` (T3) · onglet éditable + Copier + Régénérer (T4) · config éditable (T1) · texte vide sans crash (T2 test). ✓
- **Types :** `generate_caption(text) -> {description, hashtags, full}` cohérent service/serveur/UI ; `window.api.caption(text)` → `/caption`. ✓
- **Pas de placeholder.** ✓
